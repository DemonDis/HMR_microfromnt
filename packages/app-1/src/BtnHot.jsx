import React from "react";
import { hot } from 'react-hot-loader/root';

const BtnHot = () => {
  return (
    <div className="container">
    <button>BtnHot</button>
  </div>
  )
};

export default hot(BtnHot);